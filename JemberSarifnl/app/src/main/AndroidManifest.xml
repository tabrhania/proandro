<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sixth.jsfnl">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".StatusPemesananActivity"></activity>
        <activity android:name=".BuktiTransferActivity" />
        <activity android:name=".AkunActivity" />
        <activity android:name=".Pembayaran_lanjutActivity" />
        <activity android:name=".PembayaranActivity" />
        <activity android:name=".KeranjangActivity" />
        <activity android:name=".ProdukActivity" />
        <activity android:name=".BerandaActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>